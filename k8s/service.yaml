apiVersion: v1
kind: Service
metadata:
  name: logicflow-service
  namespace: logicflow
spec:
  type: ClusterIP
  selector:
    app: logicflow
  ports:
    - name: http
      port: 8080
      targetPort: http
