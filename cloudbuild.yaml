substitutions:
  _REGION: europe-west3
  _ARTIFACT_REPOSITORY: debate-platform-repository
  _IMAGE_NAME: debate-app
  _CLUSTER_NAME: debate-cluster
  _CLUSTER_LOCATION: europe-west3

options:
  logging: CLOUD_LOGGING_ONLY
  machineType: E2_HIGHCPU_8

timeout: 3600s

steps:
  - id: "tests"
    name: "python:3.11"
    entrypoint: "bash"
    args:
      - "-ceu"
      - |
        pip install uv
        uv sync --frozen
        uv run pytest -q

  - id: "render-manifests"
    name: "gcr.io/cloud-builders/kubectl"
    entrypoint: "bash"
    env:
      - "CLOUDSDK_COMPUTE_REGION=${_REGION}"
      - "CLOUDSDK_CONTAINER_CLUSTER=${_CLUSTER_NAME}"
    args:
      - "-ceu"
      - |
        cp kubernetes-manifests.yaml /workspace/rendered-k8s.yaml

  - id: "build-image"
    name: "gcr.io/cloud-builders/docker"
    args:
      - "build"
      - "-t"
      - "${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_ARTIFACT_REPOSITORY}/${_IMAGE_NAME}:${SHORT_SHA}"
      - "-t"
      - "${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_ARTIFACT_REPOSITORY}/${_IMAGE_NAME}:latest"
      - "."

  - id: "push-image-sha"
    name: "gcr.io/cloud-builders/docker"
    args:
      - "push"
      - "${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_ARTIFACT_REPOSITORY}/${_IMAGE_NAME}:${SHORT_SHA}"

  - id: "push-image-latest"
    name: "gcr.io/cloud-builders/docker"
    args:
      - "push"
      - "${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_ARTIFACT_REPOSITORY}/${_IMAGE_NAME}:latest"

  - id: "deploy-gke"
    name: "gcr.io/cloud-builders/gke-deploy"
    args:
      - "run"
      - "--filename=/workspace/rendered-k8s.yaml"
      - "--image=${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_ARTIFACT_REPOSITORY}/${_IMAGE_NAME}:${SHORT_SHA}"
      - "--location=${_CLUSTER_LOCATION}"
      - "--cluster=${_CLUSTER_NAME}"

  - id: "rollout-status"
    name: "gcr.io/cloud-builders/kubectl"
    env:
      - "CLOUDSDK_COMPUTE_REGION=${_CLUSTER_LOCATION}"
      - "CLOUDSDK_CONTAINER_CLUSTER=${_CLUSTER_NAME}"
    args:
      - "rollout"
      - "status"
      - "deployment/debate-app-deployment"
      - "--timeout=600s"

images:
  - "${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_ARTIFACT_REPOSITORY}/${_IMAGE_NAME}:${SHORT_SHA}"
  - "${_REGION}-docker.pkg.dev/${PROJECT_ID}/${_ARTIFACT_REPOSITORY}/${_IMAGE_NAME}:latest"
